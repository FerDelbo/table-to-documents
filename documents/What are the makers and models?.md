To retrieve the makers and models, you can use the following SQL query:

```sql
SELECT maker, model FROM model_list;
```

### Explanation

This query selects the `maker` and `model` columns from the `model_list` table. It will return a list of all car makers and their corresponding models.